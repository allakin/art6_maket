-var menu = {'Главная': 'main.html', 'Европа': 'inside-page.html', 'Лента новостей': 'news.html'};

nav.navbar.navbar-default
 .container-fluid
   ul.nav.navbar-nav
      each val, key in menu
        if selected === key
          li.active
            a(href=val, title=key)= key
        else
          li
            a(href=val, title=key)= key
